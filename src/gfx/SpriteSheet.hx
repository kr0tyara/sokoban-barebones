package gfx;

// List all the file names under /res/sheets you want to use as spritesheets.
// JSON and PNG of the spritesheet should have the same name.
// You can access all the imported sheets with Main.sheet.[symbol name].

@:build(macros.SpriteMacro.load(['sheet']))
class SpriteSheet
{
    public function new()
    {
        
    }
}